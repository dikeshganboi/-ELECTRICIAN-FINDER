# 📐 DEPLOYMENT ARCHITECTURE

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        PRODUCTION DEPLOYMENT                        │
│                         FREE TIER PLATFORMS                         │
└─────────────────────────────────────────────────────────────────────┘

🌐 INTERNET
    │
    ├──────────────────────────┬────────────────────────┐
    │                          │                        │
    ▼                          ▼                        ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  USER FRONTEND  │  │ ADMIN FRONTEND  │  │   BACKEND API   │
│    (Vercel)     │  │    (Vercel)     │  │    (Render)     │
├─────────────────┤  ├─────────────────┤  ├─────────────────┤
│                 │  │                 │  │                 │
│ Next.js 14      │  │ Next.js 14      │  │ Node.js         │
│ TypeScript      │  │ TypeScript      │  │ Express         │
│ TailwindCSS     │  │ TailwindCSS     │  │ Socket.IO       │
│ Google Maps     │  │ React Query     │  │ MongoDB         │
│ Socket.IO       │  │ Charts          │  │ JWT Auth        │
│                 │  │                 │  │                 │
│ Customer UI     │  │ Verification    │  │ REST API        │
│ Electrician UI  │  │ User Mgmt       │  │ WebSocket       │
│ Booking         │  │ Analytics       │  │ Role-Based      │
│ Live Tracking   │  │ Platform Ctrl   │  │ Access Control  │
│                 │  │                 │  │                 │
│ FREE TIER       │  │ FREE TIER       │  │ FREE TIER       │
│ ✓ 100GB/month   │  │ ✓ 100GB/month   │  │ ✓ 512MB RAM     │
│ ✓ Unlimited     │  │ ✓ Unlimited     │  │ ✓ Spins down    │
│   requests      │  │   requests      │  │   after 15min   │
└────────┬────────┘  └────────┬────────┘  └────────┬────────┘
         │                    │                     │
         │    HTTPS + WSS     │    HTTPS + WSS     │
         │    JWT Tokens      │    JWT Tokens      │
         │                    │                     │
         └────────────────────┴─────────────────────┘
                              │
                              ▼
                  ┌───────────────────────┐
                  │   MongoDB Atlas       │
                  │   (Cloud Database)    │
                  ├───────────────────────┤
                  │                       │
                  │ Collections:          │
                  │ • users               │
                  │ • electricians        │
                  │ • bookings            │
                  │ • verifications       │
                  │ • reviews             │
                  │                       │
                  │ FREE TIER (M0)        │
                  │ ✓ 512MB Storage       │
                  │ ✓ Shared CPU          │
                  │ ✓ Unlimited users     │
                  └───────────────────────┘
```

---

## Request Flow

### User Authentication Flow

```
[Customer Browser]
      │
      │ 1. POST /api/auth/register
      ▼
[User Frontend - Vercel]
      │
      │ 2. HTTPS Request
      │    + JSON Payload
      ▼
[Backend API - Render]
      │
      │ 3. Validate data
      │ 4. Hash password
      │ 5. Save to DB
      ▼
[MongoDB Atlas]
      │
      │ 6. Return user
      ▼
[Backend API]
      │
      │ 7. Generate JWT
      │ 8. Return tokens
      ▼
[User Frontend]
      │
      │ 9. Store tokens
      │ 10. Redirect to dashboard
      ▼
[Customer Dashboard]
```

---

### Electrician Verification Flow

```
[Electrician] → Register → [User Frontend]
                              │
                              │ Upload documents
                              ▼
                          [Backend API]
                              │
                              │ Store in MongoDB
                              │ Status: "pending"
                              ▼
                          [MongoDB Atlas]
                              │
                              │ Real-time notification
                              ▼
                       [Admin Frontend] ← Admin logs in
                              │
                              │ Review documents
                              │ Click "Approve"
                              ▼
                          [Backend API]
                              │
                              │ Update status: "verified"
                              │ Send WebSocket event
                              ▼
                 ┌────────────┴────────────┐
                 ▼                         ▼
          [Electrician]              [MongoDB Atlas]
          Receives notification      Status updated
                 │
                 │ Now appears in search
                 ▼
           [Customer Search]
```

---

### Real-Time Location Updates (Socket.IO)

```
[Electrician Mobile]
      │
      │ Enable location sharing
      │
      │ 1. Connect WebSocket
      ▼
[Backend - Socket.IO Server]
      │
      │ 2. Authenticate socket
      │ 3. Join "electrician-{id}" room
      │
      │ Periodic location updates
      ▼
[Backend broadcasts to "nearby-electricians" room]
      │
      ├────────────────┬────────────────┐
      ▼                ▼                ▼
[Customer 1]    [Customer 2]    [Customer 3]
Sees real-time  Sees real-time  Sees real-time
marker updates  marker updates  marker updates
```

---

## Security Architecture

```
┌────────────────────────────────────────────────────────────┐
│                     SECURITY LAYERS                        │
└────────────────────────────────────────────────────────────┘

Layer 1: TRANSPORT SECURITY
┌──────────────────────────────────────────────────────────┐
│ • HTTPS Everywhere (TLS 1.3)                             │
│ • WSS (WebSocket Secure)                                 │
│ • Certificate auto-managed by Vercel/Render             │
└──────────────────────────────────────────────────────────┘
                          ▼
Layer 2: API SECURITY
┌──────────────────────────────────────────────────────────┐
│ • Helmet.js (security headers)                           │
│ • CORS (whitelist only)                                  │
│ • Rate Limiting (100 req/15min)                          │
│ • Request size limits (10MB)                             │
└──────────────────────────────────────────────────────────┘
                          ▼
Layer 3: AUTHENTICATION
┌──────────────────────────────────────────────────────────┐
│ • JWT (JSON Web Tokens)                                  │
│ • Access Token: 15 min expiry                            │
│ • Refresh Token: 7 day expiry                            │
│ • bcrypt password hashing (10 rounds)                    │
└──────────────────────────────────────────────────────────┘
                          ▼
Layer 4: AUTHORIZATION
┌──────────────────────────────────────────────────────────┐
│ • Role-Based Access Control (RBAC)                       │
│ • Roles: user, electrician, admin                        │
│ • Middleware checks on every protected route             │
│ • Socket.IO authentication                               │
└──────────────────────────────────────────────────────────┘
                          ▼
Layer 5: DATABASE SECURITY
┌──────────────────────────────────────────────────────────┐
│ • MongoDB Atlas encryption at rest                       │
│ • Encryption in transit                                  │
│ • IP whitelist (or 0.0.0.0/0 for free tier)             │
│ • Strong admin password                                  │
│ • Read/write separation                                  │
└──────────────────────────────────────────────────────────┘
```

---

## Data Flow

### Authentication Flow

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ 1. Login Request
       │    { email, password }
       ▼
┌─────────────────┐
│  Backend API    │
│  Verify         │ 2. Check password hash
│  credentials    │    bcrypt.compare()
└──────┬──────────┘
       │ 3. Valid? ✓
       ▼
┌─────────────────┐
│  Generate JWT   │
│  Access: 15m    │
│  Refresh: 7d    │
└──────┬──────────┘
       │ 4. Return tokens
       ▼
┌─────────────────┐
│  Frontend       │
│  Store tokens   │ 5. localStorage.setItem()
│  in storage     │
└──────┬──────────┘
       │ 6. Include in requests
       │    Authorization: Bearer <token>
       ▼
┌─────────────────┐
│  Protected      │
│  Routes         │ 7. Verify token
└─────────────────┘    8. Decode user info
                       9. Check role
                      10. Grant/Deny access
```

---

## Deployment Pipeline

```
┌──────────────────────────────────────────────────────────┐
│                   DEVELOPMENT                            │
└──────────────────────────────────────────────────────────┘
              │
              │ git push origin main
              ▼
┌──────────────────────────────────────────────────────────┐
│                   GITHUB REPOSITORY                      │
└──────────────────────────────────────────────────────────┘
              │
              ├─────────────┬──────────────┬──────────────┐
              ▼             ▼              ▼              │
        [Vercel]      [Vercel]        [Render]           │
        Watches       Watches         Watches            │
        /frontend     /admin          /backend           │
              │             │              │              │
              ▼             ▼              ▼              │
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐     │
    │   BUILD     │ │   BUILD     │ │   BUILD     │     │
    ├─────────────┤ ├─────────────┤ ├─────────────┤     │
    │ npm install │ │ npm install │ │ npm install │     │
    │ npm build   │ │ npm build   │ │ npm build   │     │
    │ optimize    │ │ optimize    │ │ TypeScript  │     │
    └──────┬──────┘ └──────┬──────┘ └──────┬──────┘     │
           │               │               │             │
           ▼               ▼               ▼             │
    ┌─────────────┐ ┌─────────────┐ ┌─────────────┐     │
    │   DEPLOY    │ │   DEPLOY    │ │   DEPLOY    │     │
    ├─────────────┤ ├─────────────┤ ├─────────────┤     │
    │ CDN deploy  │ │ CDN deploy  │ │ Container   │     │
    │ Edge cache  │ │ Edge cache  │ │ Health check│     │
    └──────┬──────┘ └──────┬──────┘ └──────┬──────┘     │
           │               │               │             │
           ▼               ▼               ▼             │
    ┌─────────────────────────────────────────────┐     │
    │           PRODUCTION LIVE                   │     │
    │   ✓ User Frontend: https://app.vercel.app   │     │
    │   ✓ Admin Panel: https://admin.vercel.app   │     │
    │   ✓ Backend API: https://api.onrender.com   │     │
    └─────────────────────────────────────────────┘     │
                                                         │
    Auto-deploy on push ✓                               │
    Zero-downtime deployment ✓                          │
    Automatic HTTPS ✓                                   │
    Custom domains supported ✓                          │
                                                         │
    ← Rollback available (click button in dashboard) ───┘
```

---

## Cost Breakdown (FREE TIER)

```
┌──────────────────────────────────────────────────────────┐
│                   MONTHLY COST: $0                       │
└──────────────────────────────────────────────────────────┘

┌─────────────────────┬─────────────────┬────────────────┐
│ Service             │ Free Tier       │ Limitations    │
├─────────────────────┼─────────────────┼────────────────┤
│ Vercel              │ 100GB bandwidth │ Per project    │
│ (User Frontend)     │ Unlimited sites │                │
│                     │ Automatic SSL   │                │
├─────────────────────┼─────────────────┼────────────────┤
│ Vercel              │ 100GB bandwidth │ Per project    │
│ (Admin Frontend)    │ Unlimited sites │                │
│                     │ Automatic SSL   │                │
├─────────────────────┼─────────────────┼────────────────┤
│ Render              │ 512MB RAM       │ Spins down     │
│ (Backend API)       │ 0.1 CPU         │ after 15min    │
│                     │ Automatic SSL   │ Cold start     │
├─────────────────────┼─────────────────┼────────────────┤
│ MongoDB Atlas       │ 512MB storage   │ Shared cluster │
│ (Database)          │ M0 cluster      │                │
│                     │ Daily backups   │                │
├─────────────────────┼─────────────────┼────────────────┤
│ Google Maps API     │ $200/month      │ Credit expires │
│ (Maps)              │ credit          │ Never charged  │
│                     │ 28k free loads  │ with credit    │
├─────────────────────┼─────────────────┼────────────────┤
│ Razorpay            │ Test mode       │ No real        │
│ (Payments)          │ Unlimited tests │ transactions   │
└─────────────────────┴─────────────────┴────────────────┘

TOTAL COST: $0/month ✓

When to upgrade:
• >500 active users → Render Standard ($7/mo)
• >1000 users → MongoDB Atlas M2 ($9/mo)
• >100GB bandwidth → Vercel Pro ($20/mo per project)
• Production payments → Razorpay transaction fees (2%)
```

---

## Monitoring & Logging

```
┌────────────────────────────────────────────────────────┐
│                 MONITORING STACK                       │
└────────────────────────────────────────────────────────┘

[Uptime Monitoring]
    │
    └─→ UptimeRobot (FREE)
         • Ping every 5 minutes
         • Keep Render service warm
         • Email alerts on downtime
         • Status page

[Application Logs]
    │
    ├─→ Render Dashboard
    │    • Real-time logs
    │    • Last 7 days history
    │    • Error highlighting
    │
    └─→ Vercel Dashboard
         • Build logs
         • Runtime logs (Serverless Functions)
         • Error tracking

[Database Monitoring]
    │
    └─→ MongoDB Atlas
         • Query performance
         • Storage usage
         • Connection count
         • Alerts (>80% storage)

[Frontend Analytics] (Optional)
    │
    └─→ Vercel Analytics (FREE)
         • Page views
         • Core Web Vitals
         • Device breakdown
         • Geographic data

[Error Tracking] (Optional)
    │
    └─→ Sentry (FREE tier)
         • Real-time errors
         • Stack traces
         • User context
         • 5K events/month
```

---

## Backup Strategy

```
┌────────────────────────────────────────────────────────┐
│                 BACKUP STRATEGY                        │
└────────────────────────────────────────────────────────┘

[Code Backup]
    │
    └─→ GitHub Repository
         • Full version history
         • Branch protection
         • Automatic via git

[Database Backup]
    │
    └─→ MongoDB Atlas (Automatic)
         • Daily snapshots (Free tier)
         • 2 snapshots retained
         • Point-in-time recovery (Paid)
         • Manual export available

[Environment Variables]
    │
    └─→ Manual Documentation
         • Keep .env.template files
         • Secure storage (password manager)
         • Team access control

[Recovery Plan]
    │
    ├─→ Code: Redeploy from GitHub
    ├─→ Database: Restore from Atlas snapshot
    └─→ Env vars: Re-enter from secure storage
```

---

## Scaling Path

```
Current: FREE TIER
    ↓
    │ >500 active users
    ↓
TIER 1: Render Standard ($7/mo)
    • No sleep/spin-down
    • 512MB RAM persistent
    • Faster cold starts
    ↓
    │ >1000 users
    ↓
TIER 2: Add MongoDB M2 ($9/mo)
    • 2GB storage
    • Dedicated CPU
    • Better performance
    ↓
    │ >5000 users
    ↓
TIER 3: Vercel Pro ($20/mo each)
    • Team features
    • Advanced analytics
    • Priority support
    ↓
    │ >10000 users
    ↓
TIER 4: Production Grade
    • Render Pro ($25/mo)
    • MongoDB M10 ($57/mo)
    • Redis cache ($10/mo)
    • CDN (Cloudflare Pro $20/mo)
    • Total: ~$150/mo
```

---

**Architecture Version:** 1.0.0  
**Last Updated:** January 15, 2026
