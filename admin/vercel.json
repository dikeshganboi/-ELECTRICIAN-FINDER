# Vercel Configuration for Admin Frontend
{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_API_BASE_URL": {
      "type": "string",
      "description": "Backend API URL"
    }
  }
}
